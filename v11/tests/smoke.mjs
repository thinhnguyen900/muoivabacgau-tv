import { chromium, webkit } from '@playwright/test';
const url=process.env.V11_URL||'http://127.0.0.1:4173';
for(const engine of [chromium,webkit]){const browser=await engine.launch({headless:true});const page=await browser.newPage({viewport:{width:1280,height:800}});const errors=[];page.on('pageerror',e=>errors.push(String(e)));page.on('console',m=>{if(m.type()==='error'&&!m.text().includes('bac-gau.glb'))errors.push(m.text())});await page.goto(url,{waitUntil:'networkidle',timeout:45000});await page.waitForFunction(()=>window.__BACGAU_V11__?.ready===true,null,{timeout:15000});const state=await page.evaluate(()=>({ready:document.querySelector('.app')?.dataset.appReady,stage:document.querySelector('#stage')?.getBoundingClientRect().toJSON(),v11:window.__BACGAU_V11__}));if(state.ready!=='true'||state.stage.width<500||state.stage.height<400)throw new Error(`${engine.name()} layout gate failed ${JSON.stringify(state)}`);if(errors.length)throw new Error(`${engine.name()} errors: ${errors.join(' | ')}`);await browser.close();console.log(engine.name(),'PASS',JSON.stringify(state.v11))}
